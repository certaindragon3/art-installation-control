No more than 10 seconds for each created Json/
Implement on server backend the C - sharp code for Currency and score
Double-check track list payload
Players alwasy need to be able to play the tracks when shown
[Probably already done!!!!]The movement of students is controlled with 
{
"student id"
"start_position" Vector2
"end_position" Vector2
"interpolation_time" [if this is 5 seconds]
"loop_times" , if 0 means no loop [int : how many times]
}
Could be useful: loop movement for some time
Double-check the multiple users with same name: I saw a bug[Don't spend time before you did anything else; try and use device id to screen on false duplicates]
Attached device screenshot for baseline UI look
Oh, don't expose the controller to students. That could be just by using a dedicated address.